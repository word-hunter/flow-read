name: flow_read_atmosphere
description: City time theme and low-distraction atmosphere widgets for Flow Read reading surfaces.
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
