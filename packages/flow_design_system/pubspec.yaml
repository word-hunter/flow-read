name: flow_design_system
description: Cross-platform composable Design System for Flow Read. Provides tokens, palettes, shells, and theme building.
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
